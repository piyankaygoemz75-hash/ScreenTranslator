<Window x:Class="ScreenTranslator.App.Windows.ContinuousSidePanelWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:behaviors="clr-namespace:ScreenTranslator.App.Behaviors"
        Width="420"
        Height="620"
        MinWidth="340"
        MinHeight="320"
        MaxWidth="560"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True">
    <Border Margin="14"
            Background="{DynamicResource TranslationSurfaceBrush}"
            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="12">
        <Border.Effect>
            <DropShadowEffect Color="Black"
                              BlurRadius="24"
                              ShadowDepth="6"
                              Opacity="0.28" />
        </Border.Effect>
        <Grid behaviors:MouseWheelRouter.IsEnabled="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid x:Name="HeaderArea"
                  Margin="18,14,10,12"
                  Background="Transparent"
                  Cursor="SizeAll"
                  MouseLeftButtonDown="Header_OnMouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="连续翻译"
                               FontSize="16"
                               FontWeight="SemiBold" />
                    <TextBlock Text="{Binding StatusText}"
                               Margin="0,3,0,0"
                               FontSize="12"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                </StackPanel>
                <ui:Button Grid.Column="1"
                           Command="{Binding CloseCommand}"
                           Appearance="Transparent"
                           Cursor="Arrow"
                           Padding="8"
                           ToolTip="关闭">
                    <ui:SymbolIcon Symbol="Dismiss20" />
                </ui:Button>
            </Grid>

            <ScrollViewer Grid.Row="1"
                          Margin="10,0,8,0"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding Results}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="4,4,8,8"
                                    Padding="14"
                                    CornerRadius="9"
                                    Background="{DynamicResource ControlFillColorSecondaryBrush}"
                                    BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                    BorderThickness="1">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="{Binding SourceText}"
                                               FontSize="12"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                               TextWrapping="Wrap"
                                               MaxHeight="72" />
                                    <TextBlock Grid.Row="1"
                                               Text="{Binding TranslatedText}"
                                               Margin="0,9,0,0"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               TextWrapping="Wrap" />
                                    <StackPanel Grid.Row="2"
                                                Margin="0,12,0,0"
                                                Orientation="Horizontal"
                                                HorizontalAlignment="Right">
                                        <ui:Button Content="复制"
                                                   Command="{Binding CopyCommand}"
                                                   Padding="12,5"
                                                   Margin="0,0,8,0" />
                                        <ui:Button Content="清除此项"
                                                   Command="{Binding CloseCommand}"
                                                   Padding="12,5" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <Grid Grid.Row="2"
                  Margin="18,10,18,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Esc 只结束框选，队列会继续完成"
                           FontSize="12"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           VerticalAlignment="Center" />
                <ui:Button Grid.Column="1"
                           Content="清除全部"
                           Command="{Binding ClearAllCommand}"
                           Padding="14,6" />
            </Grid>
        </Grid>
    </Border>
</Window>
